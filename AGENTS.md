# Agent 约束

## 未经允许不准改代码

- 未经用户明确允许，禁止修改用户的代码。
- 只允许基于现有代码做原因分析、给出结论与修复建议；不得自行落地补丁。
- 即使用户描述了 bug、指出了行号，或 Agent 已判断出修复点，在未得到「可以改」之前也只输出分析。
- 用户明确允许修改后才能改代码。

### 例外：前端（`fe/`）可直接改

- 涉及 `fe/` 目录内的前端问题（渲染、样式、交互、依赖等），Agent 可直接修改并落地修复，无需再征求「可以改」。
- 后端、共享逻辑、配置、基础设施等仍遵守上方「未经允许不准改代码」约束。

## 前端视觉规范：Pelican「海岸疗愈 · 扁平版」

当需要手写结构或自定义样式（图表、插画、空状态、登录页、营销区块等 Ant Design Token 覆盖不到的地方）时，必须遵守本规范。

核心原则：能用 Ant Design 组件和 Token 的一律使用；必须手写时，颜色和度量从本规范取值，不凭感觉新增；全站无阴影，分层只靠边框和色阶。

### 色板

| 用途 | 色值 |
| --- | --- |
| 品牌主色（墨绿） | `#4A7C6F` |
| 主色深（hover / 强调 / 压态） | `#3E6B5E` / `#2E5347` |
| 选中底 | `#E4EDE7` |
| hover 底 | `#EEF3EF` |
| 页面底色（奶油） | `#F5F1E6` |
| 容器（浅奶油） | `#FBF8F0` |
| 弹层 | `#FCFAF3` |
| 表头 / 浅色块 | `#F1EADB` |
| 主文字 | `#2E3A34` |
| 标题文字 | `#22302A` |
| 次要文字 | `#6B7B72` |
| 边框 | `#E4DCC9` |
| 分割线 | `#EBE4D3` |
| 状态暖黄 | `#E0A63C` |
| 状态橙红 | `#D96A4A` |

图表调色板按序使用：`['#4A7C6F', '#7FA99B', '#E0A63C', '#D96A4A', '#8C9E76']`。

- 单色图表使用 `#4A7C6F`；面积渐变使用 `rgba(74,124,111,0.16)` 到 `rgba(74,124,111,0)`。
- 网格线和坐标轴使用 `#E4DCC9` 虚线；刻度文字使用 `#6B7B72`。

### 度量

- 圆角：小 8px、常规 12px、大 16px。禁用直角和 999px 胶囊圆角。
- 间距：使用 8 的倍数（8 / 16 / 24 / 32px）；区块间距 24–32px，卡片内边距 32px，元素间距 8–16px。
- 描边：`1px solid #E4DCC9`，分割线使用 `#EBE4D3`。

### 分层11

- 全站禁止 `box-shadow`、`drop-shadow` 和 `filter` 投影。
- 层级区分按以下优先级：1px 边框；奶油色阶（页面 `#F5F1E6`、容器 `#FBF8F0`、弹层 `#FCFAF3`）；间距留白。
- 下拉、弹窗和气泡使用 1px 边框及 `#FCFAF3` 背景界定轮廓。
- 悬浮态不使用阴影，只使用 `#EEF3EF` 背景或加深边框。

### 字体

- 正文和 UI：`'Noto Sans SC', sans-serif`，15px。
- 展示标题、品牌字和大数字：`'Noto Serif SC', serif`，字重 600，字距 `.02em`，使用 `.page-title`。
- 英文小标签 eyebrow：全大写，字距 `.18em`，12px，`#4A7C6F`，字重 600，使用 `.eyebrow`。
- 层级顺序：eyebrow 小标签、衬线主标题、`#6B7B72` 次要文字副标题。

### 气质

- 留白优先、低饱和莫兰迪、扁平克制，整体松弛、慢节奏、冷淡。
- 插画采用扁平矢量、干净描边和柔和填充，不加高光或光晕；线条端点使用圆角。
- 暖黄和橙红只用于小面积点缀，如标签、图标和单个数据点。

### 落地规则

1. 优先使用 Ant Design 组件和 Token，不重复手写已有组件能力。
2. 手写样式只能使用上述颜色和度量，不引入规范外新色。
3. 手写 CSS 只作用于自定义 class，禁止通过 `.ant-*` 选择器覆盖组件。
4. 禁止给 Ant Design 组件添加硬编码内联视觉样式（颜色、背景、边框、圆角、阴影、字体）；内联布局样式（margin、padding、width、flex、gap）可以使用。
5. 全站不出现阴影；交互态复用 `#EEF3EF`、`#E4EDE7`、`#3E6B5E`。
